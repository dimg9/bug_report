import { ReporterUppercasePipe } from './reporter-uppercase.pipe';

describe('ReporterUppercasePipe', () => {
  it('create an instance', () => {
    const pipe = new ReporterUppercasePipe();
    expect(pipe).toBeTruthy();
  });
});
